ALTER TABLE n003
    ADD COLUMN IF NOT EXISTS ins003 VARCHAR(120),
    ADD COLUMN IF NOT EXISTS tok003 TEXT,
    ADD COLUMN IF NOT EXISTS cli003 TEXT,
    ADD COLUMN IF NOT EXISTS out003 BOOLEAN NOT NULL DEFAULT FALSE;

CREATE TABLE IF NOT EXISTS n018 (
    cod018 BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    cod001 BIGINT NOT NULL REFERENCES n001(cod001) ON DELETE CASCADE,
    cod003 BIGINT NOT NULL REFERENCES n003(cod003) ON DELETE CASCADE,
    cod009 BIGINT NOT NULL REFERENCES n009(cod009) ON DELETE CASCADE,
    sta018 VARCHAR(30) NOT NULL,
    ext018 VARCHAR(255),
    err018 TEXT,
    cad018 TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
    atu018 TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT uq_n018_mensagem UNIQUE (cod009)
);

CREATE INDEX IF NOT EXISTS ix_n018_empresa_data ON n018 (cod001, cad018 DESC);
CREATE INDEX IF NOT EXISTS ix_n018_mensagem ON n018 (cod009);

COMMENT ON COLUMN n003.ins003 IS 'ID da instância Z-API do canal WhatsApp.';
COMMENT ON COLUMN n003.tok003 IS 'Token da instância Z-API criptografado.';
COMMENT ON COLUMN n003.cli003 IS 'Client-Token da Z-API criptografado.';
COMMENT ON COLUMN n003.out003 IS 'Habilita o envio de respostas humanas pelo canal.';
COMMENT ON TABLE n018 IS 'Tentativas de entrega de mensagens enviadas pelo Nexora a canais externos.';
